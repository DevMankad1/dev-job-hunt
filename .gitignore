node_modules/
*.log
.DS_Store
.env
.env.*
# Tailored resumes are generated per application — regenerate, don't commit.
resume/tailored/*
!resume/tailored/.gitkeep

# Windows crash dumps
*.stackdump

# Standing-resume build artefacts (regenerate with `npm run resumes`)
resume/*.jd.txt

resume/*.plan.json

# Excel lock files
~$*
